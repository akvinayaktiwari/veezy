{
  "name": "veezy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:docker": "docker compose -f docker-compose.prod.yml up --build",
    "dev:docker:down": "docker compose -f docker-compose.prod.yml down",
    "build:prod": "turbo run build",
    "deploy:aws": "npm run docker:build && npm run docker:push && npm run ecs:deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "private": "true",
  "packageManager": "npm@11.3.0",
  "devDependencies": {
    "turbo": "^2.6.3"
  }
}
